/**
 * Module Template - Type Definitions
 * 
 * Re-export shared types và định nghĩa types riêng cho module.
 */

// Re-export shared types để sử dụng thuận tiện
export { Role, type User } from '../../shared/types/common.ts';

// Thêm types riêng cho module ở đây:
// export interface MyEntity {
//   id: string;
//   name: string;
//   ...
// }
