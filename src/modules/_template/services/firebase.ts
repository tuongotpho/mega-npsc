/**
 * Module Template - Firebase Service
 * 
 * Nếu module sử dụng Firebase project riêng, cấu hình ở đây.
 * Nếu dùng chung Firebase với module khác, import trực tiếp từ module đó.
 */

// import firebase from 'firebase/compat/app';
// import 'firebase/compat/auth';
// import 'firebase/compat/firestore';
// import 'firebase/compat/storage';

// const firebaseConfig = {
//   apiKey: "...",
//   authDomain: "...",
//   projectId: "...",
//   storageBucket: "...",
//   messagingSenderId: "...",
//   appId: "...",
// };

// const app = firebase.apps.find(app => app.name === 'module-name')
//   || firebase.initializeApp(firebaseConfig, 'module-name');

// export const db = app.firestore();
// export const auth = app.auth();
// export const storage = app.storage();
